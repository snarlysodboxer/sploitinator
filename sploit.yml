# Restart Sploit if you change this file
sploit:
  watchdir: "host.d"
  modulesfile: "modules.yml"
  serveaddress: "127.0.0.1:8080"
  updatespec: "@every 1d" # when to update MSF with git
  statusspec: "@every 1d" # when to send a status email
  loginfo: "sploitInfo.log"
  logdebug: "sploitDebug.log" # set to "" for no debug logging
msfrpc:
  uri: "http://localhost:3790/api/1.0"
  user: "admin"
  pass: "mypass"
postgres:
  db: "msf"
  user: "postgres"
  host: "localhost"
  port: "5432"
smtp:
  user: "admin@example.com"
  pass: "mypass"
  host: "smtp.example.net:587"
  from: "msf@example.com"
  to: "admin1@example.com admin2@example.com"
